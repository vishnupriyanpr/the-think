# Backend API base URL
# For single-service deployment (backend serves frontend), use /api
# For separate services, use the full backend URL e.g. https://your-backend.onrender.com/api
VITE_API_URL=/api
